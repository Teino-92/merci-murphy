## DO

- Follow STACK.md exactly (no alternatives)
- Keep code simple and readable
- Ask if requirements are unclear
- Test your changes work
- Update ROADMAP.md after each task
- Use next/image for ALL images (never raw <img>)
- Use Server Components by default, Client Components only when needed (interactivity, hooks)
- Validate ALL form inputs with Zod
- Store secrets in .env.local only — never hardcode

## DO NOT

- Add features not in ROADMAP.md or SCOPE.md
- Change the tech stack
- Skip tasks without approval
- Over-engineer solutions
- Suggest "improvements" outside scope
- Write documentation unless asked
- Use any TypeScript (never use `any`)
- Add console.log in production code
